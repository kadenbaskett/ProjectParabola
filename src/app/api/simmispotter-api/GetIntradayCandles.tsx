import axios, { AxiosReponse } from "axios";

export interface GetCandlesReponse {}

export const getIntradayCandles = async (
  ticker: string,
  interval: number,
  startTimestamp: number,
  endTimestamp: number
) => {
  try {
  } catch (error: any) {}
};
